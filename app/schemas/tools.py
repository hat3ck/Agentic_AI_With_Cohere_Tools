from typing import Dict

WIKIPEDIA_TOOL_SPEC = {
    "name": "wikipedia",
    "description": "Fetch a short summary from Wikipedia based on a query",
    "parameters": {
        "query": "string"
    }
}